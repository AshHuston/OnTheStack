export function ensureNewestSet() {
    /* 
    Get newest set that has had its prerelase date pass.(Release-1wk)
        Check if it is aleady in our newSet.json --> Early return

    Check set_type to ensure its acceptable.
        iF NOT, EARLY RETRUN

    Build cardpool with its cards.

    Write it to the pool.

    Maybe set flag for some kind of announcment callout or sumn?
    */
}